import React from 'react';
import { View, Text } from 'react-native';

const MatchScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Match Screen</Text>
      {/* Add AI conversation display here */}
    </View>
  );
};

export default MatchScreen;
