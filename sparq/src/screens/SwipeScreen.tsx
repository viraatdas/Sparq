import React from 'react';
import { View, Text } from 'react-native';

const SwipeScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Swipe Screen</Text>
      {/* Add swiping functionality here */}
    </View>
  );
};

export default SwipeScreen;
